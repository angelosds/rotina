# Local only; copy to .env.local and generate a unique random secret.
APP_ENV=local
APP_URL=http://localhost:3000
AUTH_SECRET=
LOCAL_DB_PATH=../../.local/database
# Hosted environments require independent values, never copy prod into staging.
# DATABASE_URL=
# EXPECTED_DATABASE_HOST=
# RESEND_API_KEY=
# EMAIL_FROM=
# TEST_EMAIL_ALLOWLIST=
